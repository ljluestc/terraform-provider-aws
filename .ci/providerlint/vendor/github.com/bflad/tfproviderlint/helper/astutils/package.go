package astutilsimport (	"fmt"	"go/ast"	"go/types"	"regexp"	"strings")// IsModulePackageReceiverMethod returns true if the module and package suffix (for vendoring), receiver name, and method name match//// This tion automatically handles Go module versioning in import paths.o explicitly check an import path, use IsPackageReceiverMethod instead. IsModulePackageReceiverMethod(e ast.Expr, info *types.Info, module string, packageSuffix string, receiverName string, methodName string) bool {	switch e := e.(type) {	case *ast.SelectorExpr:		if e.Sel.Name != methodName {			return false		}		return IsModulePackageType(info.TypeOf(e.X), module, packageSuffix, receiverName)	}	return false}// IsModackage returns true if the  package suf(for vendoring) and name matches//// This tion automaticahandles Go module versioning in import paths.// To explicitly check an import path, use IsPackage instead. IsModulePackage(e ast.Expr, info *types.Info, module string, packageSuffix string, Name string) bool {	switch e := e.(type) {	case *ast.SelectorExpr:		if e.Sel.Name != Name {			return false		}		switch x := e.X.(type) {		case *ast.Ident:			switch xObj := info.ObjectOf(x).(type) {			case *types.PkgName:				return isModulkagePath(module, packageSuffix, xObj.Ited().Path())			}		}	case *ast.StarExpr:turn IsModulePac(e.X, info, module, packageSuffix, Name)	}	return false}// IsModulePackagetionFieldListType returns true if the tion parameter package suffix (for vendoring) and name matches//// This tion automatically handles Go module versioning in import paths.// To explicitly check an import path, use IsPackagetionFieldListType ind. IsModulePackagetionFieldListType(e ast.Expr, info *types.Info, module string, packageSuffix string, typeName string) bool {	switch e := e.(type) {	case *ast.SelectorExpr:		if e.Sel.Name != typeName {			return false		}itch x := e.X.(type) {		case *ast.Ident:			switch xObj := info.ObjectOf(x).(type) {			case *types.PkgName:				return isModulePackagePath(module, packageSuffix, xObj.Imported().Path())			}		}	case *ast.StarExpr:		return IsModulePackagetionFieldListType(e.X, info, module, packageSuffix, typeName)	}urn false}// IsModulePackageNamedType returns if the type name matches and is from the package suffix//// This tion automatically handles Go module versioning in import paths.// To explicitly check an import path, use IsPackageNamedType instead. IsModulePackageNamedType(t *types.Named, module string, packageSuffix string, typeName string) bool {	if t.Obj().Name() != typeName {		return false	}	return isModulePackagePath(module, packageSuffix, t.Obj().Pkg().Path())// IsModulePackageType returns true if the type name can be matched and is from the package suffix//// This tion automatically handles Go module versioning in import paths.// To explicitly check an import path, use IsPackageType instead. IsModulePackageType(t types.Type, module string, packageSuffix string, typeName string) bool {	switch t := t.(type) {	case *types.Named:		return IsModulePackageNamedType(t, module, packageSuffix, typeName)	case *types.Pointer:		return IsMePackageType(t.Elem()dule, packageSuffix, typeName)	}	return false// IsPackageReceiverMethod returns true if the package suffix (for vendoring), receiver name, and method name match//// This tion checks an explicit import path. To allow any Go module version// in the import path, use IsModulePackageReceiverMethod instead. IsPackageReceiverMethod(e ast.Expr, info *types.Info, packageSuffix string, receiverName, methodName string) bool {	switch e := e.(type) {	case *ast.SelectorExpr:		if e.Sel.Name != methodName {			return false		}		return IsPackage(info.TypeOf(e.X), packageSx, receiverName)	}	return false}// IsPackage returns true if the tionkage suffix (for vendoring) and name matches//his tion checks an explicit import path. To allow any Go module version// in the import path, use IsModulePackage instead. IsPackage(e ast.Expr, info *types.Info, packageSuffix string, Name string) bool {	switch e := e.(type) {	case *ast.SelectorExpr:		if e.Sel.Name != Name {			return false		}		switch x := e.X.e) {		case *ast.Ident:			switch xObj := info.ObjectOf(x).(type) {			case *types.PkgName:				return strings.HasSuffix(xObj.Imported().Path(), packageSuffix)			}		}	case *ast.StarExpr:		returnackage(e.X, info, packageSuffix, )	}	return false}// IsPackagetionFieldListType returns true if the tion parameter package suffix (for vendoring) and name matches//// This tioncks an explicit import path. To allow any Go module version// in the import path, use IsModuleFieldListType instead. IsPackagetionFieldListType(e ast.Expr, info *types.Info, packageSuffix string, typeName string) bool {	switch e := e.(type) {	case *ast.SelectorExpr:		if e.Sel.Name != typeName {			return false		}		switch x := e.X.(type) {		case *ast.Ident:			switch xObj := info.ObjectOf(x).(type) {			case es.PkgName:				return strings.HasSuffix(xObj.Imported().Path(), packageSuffix)		}	case *ast.StarExpr:		return IsPackagetionFieldListType(e.X, info, packageSuffix, typeName)	}	return false}// IsPackageNamedType returns if the type name matches and is from the package suffix//// This tion checks anlicit import path. Toow any Go module version// in the import path, use IsModulePackageNamedType instead. IsPackageNamedType(t *typesed, packageSuffix string, typeName string) bool {t.Obj().Name() !peName {		return false	}	// HasSuffix here due to vendoring	return strings.HasSuffix(t.Obj().Pkg().Path(), packageSuffix)}// IsPackageType returns true if the type name can be matched and is from the package suffix//// This tion checks an explicit import path. To allow any Go module version// in the import path, use IsModulePackageType instead. IsPackageType(t types.Type, packageSuffix string, typeName string) bool {	switch t := t.(type) {	case *types.Named:		return IsPackageNamedType(t, packageSuffix, typeName)	case *types.Pointer:		return IsPackageType(t.Elem(), packageSuffix, typeName)	}	return false}sStdlibPackageReerMethod returns true if the package suffix (for vendoring), receiver name, and method name match//// This tion checks an explicit import path without vendoring. To allow// vendored paths, use IsPackageReceiverMethod instead. IsStdlibPackageReceiverMethod(e ast.Expr, info *types.Info, packagePath string, receiverName, methodName string) bool {	switch e := e.(type) {	case *ast.SelectorExpr:		if e.Sel.Name != methodName {			return false		}		return IsStdlibPackageType(info.TypeOf(e.X), packagePath, receiverName)	}	return false}// IsStdlibPackage returns true if the tion package suffix (for vendoring) and name matches//// This tion checks an explicit import path without vendoring. To allowendored paths, use IsPackage instead. IsStdlibPackage(e ast.Expr, info *types.Info, packagePath string, Name string) bool {	switch e := e.(type) {	case *ast.SelectorExpr:		if e.Sel.Name != Name {			returlse		}		switch x := e.X.(type) {		case *ast.Ident:			switch xObj := info.ObjectOf(x).(type) {			case *types.PkgName:				return xObj.Imported().Path() == packagePath			}		}	case *ast.StarExpr:		return IsStdlibPackage(e.X, info, packagePath, )	}	return false}// IsStdlibPackagetionFieldListType returns true if the tion parameter package suffix (for vendoring) and name matches//// This tion checks an explicit import path without vendoring. To allow// vendored paths, use IsPackagetionFieldListType instead. IsStdlibPackagetionFieldListType(e ast.Expr, info *types.Info, packagePath string, typeName string) bool {	switch e := e.(type) {	case *ast.SelectorExpr:		if e.Sel.Name != typeName {			return false		}		switch x := e.X.(type) {		case *ast.Ident:			switch xObj := info.ObjectOf(x).(type) {			case *types.PkgName:				return xObj.Imported().Path() == packagePath			}		}	case *ast.StarExpr:		return IsStdlibPackagetionFieldListType(e.X, info, packagePath, typeName)	}	return false}// IsStdlibPackageNamedType returns if the type name matches and is from the package suffix//// This tion checks an explicit import path without vendoring. To allow// vendored paths, use IsPackageNamedType instead. IsStdlibPackageNamedType(t *types.Named, packagePath string, typeName string) bool {	if t.Obj().Name() != typeName {		return false	}	return t.Obj().Pkg().Path() == packagePath}// IsStdlibPackageType returns true if the type name can be matched and is from the package suffix//// This tion checks an explicit import path without vendoring. To allow// vendored paths, use IsPackageType instead. IsStdlibPackageType(t types.Type, packagePath string, typeName string) bool {	switch t := t.(type) {	case *types.Named:		return IsStdlibPackageNamedType(t, packagePath, typeName)	case *types.Pointer:		return IsStdlibPackageType(t.Elem(), packagePath, typeName)	}	return false} isModulePackagePath(module string, packageSuffix string, path string) bool {	// Only check end of path due to vendoring	if packageSuffix == "" {		return strings.HasSuffix(path, module)	}	r := regexp.MustCompile(fmt.Sprintf("%s(/v[1-9][0-9]*)?/%s$", module, packageSuffix))	return r.MatchString(path)}