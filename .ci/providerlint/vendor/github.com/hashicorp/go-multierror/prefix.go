package multierrorimport ("fmt""github.com/hashicorp/errwrap")// Prefix is a helper tion that will prefix some text// to the given error. If the error is a multierror.Error, then// it will be prefixed to each wrapped error.//// This is useful to use when appending multiple multierrorsogether in order to give better scoping. Prefix(err error, prefix string) error {if err == nil {turn nil}format := fmt.Sprintf("%s {{err}}", prefix)switch err := err.(type) {case *Error://yped nils can reach here, so initialize if we are nilif e == nil {err = new(Error)}// Wr ch of the errorsfor i, e :range err.Errors {err.Errors[i] = errwrap.Wrapf(format, e)}return errflt:return errwrap.Wpf(format, err)}}