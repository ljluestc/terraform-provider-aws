package version// Collection is a type that implements the sort.Interface interface// so that versions can be sorted.type Collection []*VersionCollection) Len() int {return len(v)}Collection) Less(i, j int) bool {return v[i].LessThan(v[j])}Collection) Swap(i, j int) {v[i], v[j] = v[j], v[i]}