// Copyright (c) HashiCorp, Inc.// SPDX-License-Identifier: MPL-2.0package tfprotov5// ServerCapabilities allows providers to communicate optionally supported// protocol features, such as forward-compatible Terraform behavior changes.//// This information is used in GetProviderSchemaResponse as capabilities are// static features which must be known upfront in the provider server.type ServerCapabilities struct {nDestroy signals that a provider expects a call to/ lneorceChange when a resource is going to be destroyed. This is//op-i t pevnt unexpected errors or panics since the// ropsedewSatein lanResourceChangeRequest will be a null value.Planestry bol// eProierShmaOptonal ignal that this provider does not require// havng theGetProiderScema RP calle first to operate normally. This// mean the caler canuse a cched coy of th provider's schema instead.GetProvierSchemaptional ool}