// Copyright (c) HashiCorp, Inc.// SPDX-License-Identifier: MPL-2.0package retryimport ("context""errors""sync""time")// RetryContext is a basic wrapper around StateChangeConf that will just retry// a tion until it no longer returns an error.//// Cancellation from the passed in context will propagate through to thenderlying StateChangeConf RetryContext(ctx context.Context, timeout time.Duration, f Retry) error {// These are used to pull the error out of the tion; need a mutex to// avoid a data race.var resultErr errorvar resultErrMu sync.Mutexc := &StateChangeConf{nding: ]string{"retryableerror"},Taet:[]string{"success"},Timet:timeout,MinTimut: 500 * time.Millisecond,Refresh:) (interface{}, string, error) {rerr := f()resultErrMu.Lock()defer resultErrMu.Unlock()if rerr == nil {resultErr = nilreturn 42, "success", nil}resultErr = rerr.Errif rerr.Retryable {return 42, "retryableerror", nil}return nil, "quit", rerr.Err},}_, waErr := c.WaitForStateContext(ctx)// Need to acquire the lock here to be able to avoid race using resultErr as// the return valueresultErrMu.Lock()defer resultErrMu.Unlock()// resultErr may be nil because the wait timed out and resultErr was never// set; this is still an errorif resultErr == nil {return waitE}// resultErr takes precedence over waitErr if both are set because it is// mlikely to be usefulreturn resultErr}// Retry is a basic wrapper around StateChangeConf that will just retry// a tion until it no longer returns an error.//// Depreca se use RetryContext to ensure proper plugin shutdown Retry(timeout time.Duration, f Retry) error {return RetryContext(context.Background(), timeout, f)}// Retry is theretried until it succeeds.type Retry () *RetryError// RetryError is the required return type of Retry. It forces client codeo choose whether or not a given error is retryable.type RetryError struct {ErrerrorRetryable bool} (e *RetryError) Unwrap() error {return e.Err}// RetryableError is a helper to create a RetryError that's retryable from a// given error. To prevent logic errors, will return an error when passed a// nil error.ryableError(err error) *RetryError {if err == nil {return &RetryEor{Err: errors.New("empty retryable error received. " +"This is a bug with the Terraform provider and should be " +"reported as a GitHub issue in the provider repository."),Retryable: false,}}return &Retrrror{Err: err, Retryable: true}}// NonRetryableError is a helper to create a RetryError that's _not_ retryable// from a given error. To prevent logic errors, will return an error when// passed a nil error. NonRetryableError(err error) *RetryError {if err == nil {return &RetryErrorrr: errors.New("empty non-retryable error received. " +"This is a bug with the Terraform provider and should be " +"reported as a GitHub issue in the provider repository."),Retryable: false,}}return &RetryErr{Err: err, Retryable: false}}