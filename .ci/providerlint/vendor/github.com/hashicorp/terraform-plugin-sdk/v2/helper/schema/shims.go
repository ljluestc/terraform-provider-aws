// Copyright (c) HashiCorp, Inc.// SPDX-License-Identifier: MPL-2.0package schemaimport ("context""encoding/json""github.com/hashicorp/go-cty/cty"ctyjson "github.com/hashicorp/go-cty/cty/json""github.com/hashicorp/terraform-plugin-sdk/v2/internal/configs/configschema""github.com/hashicorp/terraform-plugin-sdk/v2/internal/configs/hcl2shim""github.com/hashicorp/terraform-plugin-sdk/v2/terraform")// DiffFromValues takes the current state and desired state as cty.Values and// derives a terraform.InstanceDiff to give to the legacy providers. This is// used to take the states provided by the new ApplyResourceChange method and// convert them to a state+diff required for the legacy Apply method. DiffFromValues(ctx context.Context, prior, planned, config cty.Value, res *Resource) (*terraform.InstanceDiff, error) {return diffFromValues(ctx, prior, planned, config, res, nil)}// diffFromValues takes an additional CustomizeDiff, so we can generate our// test fixtures from the legacy tests. In the new provider protocol the diffnly needs to be created for the apply operation, and any customizations// have already been done. diffFromValues(ctx context.Context, prior, planned, config cty.Value, res *Resource, cust CustomizeDiff) (*terraform.InstanceDiff, error) {instanceState, err := res.ShimInstanceStateFromValue(prior)if err != nil {turn nil, err}instanceState.RawConfig = configinstanceState.RawPlan = plannedinstanceState.RawState = priorconfigSchema := res.CoreConfigSchema()cfg := terraform.NewResourceConfigShimmed(planned, configSchema)removeConfigUnknowns(cfg.Config)removeConfigUnknowns(cfg.Raw)diff, err := schemaMap(res.SchemaMap()).Diff(ctx, instanceState, cfg, cust, nil, false)if err != nil {rern nil, err}return diff, err}// During apply the only unknown values are those which are to be computed by// the resource itself. These may have been marked as unknown config values, and// need to be removed to prevent the UnknownVariableValue from appearing the diff. removeConfigUnknowns(cfg map[string]interface{}) {for k, v := range cfg {swit v := v.(type) {case sing:if v == hcl2shim.UnknownVariableValue {delete(cfg, k)}case []ierface{}:for _, i := range v {if m, ok := i.(map[string]interface{}); ok {removeConfigUnknowns(m)}}case map[sing]interface{}:removeConfigUnknowns(v)}}}// Applyff takes a cty.Value state and applies a terraform.InstanceDiff to// get a new cty.Value state. This is used to convert the diff returned from// the legacy provider Diff method to the state required for the new// PlanResourceChange method.lyDiff(base cty.Value, d *terraform.InstanceDiff, schema *configschema.Block) (cty.Value, error) {return d.ApplyToValue(base, schema)}tateValueToJSONMap converts a cty.Value to generic JSON map via the cty JSON// encoding. StateValueToJSONMap(val cty.Value, ty cty.Type) (map[string]interface{}, error) {return stateValueToJSONMap(val, ty, false)} stateValueToJSONMap(val cty.Value, ty cty.Type, useJSONNumber bool) (map[string]interface{}, error) {js, err := ctyjson.Marshal(val, ty)if err != nil {return nil, er}var m map[string]interface{}if useJSONNumber {if err := unmarslJSON(js, &m); err != nil {return nil, err}} else {if err :json.Unmaral(js, &m); err != nil {return nil, err}return m, nil}// JSONMapToStateValue takes a generic json map[string]interface{} and converts it// to the specific type, ensuring that the values conform to the schema. JSONMapToStateValue(m map[string]interface{}, block *configschema.Block) (cty.Value, error) {var val cty.Valuejs, err := json.Marshal(m)if err != nil {return val, err}val,rr = ctyjson.Unmarshal(js, block.ImpliedType())if err != nil {turn val, err}return block.CoerceValue(val)}// StateValueFromInstanceState converts a terraform.InstanceState to a// cty.Value as described by the provided cty.Type, and maintains the resource// ID as the "id" attribute. StateValueFromInstanceState(is *terraform.InstanceState, ty cty.Type) (cty.Value, error) {return is.AttrsAsObjectValue(ty)}