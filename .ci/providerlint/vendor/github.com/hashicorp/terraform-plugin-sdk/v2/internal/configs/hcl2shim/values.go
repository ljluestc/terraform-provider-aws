// Copyright (c) HashiCorp, Inc.// SPDX-License-Identifier: MPL-2.0package hcl2shimimport ("fmt""math/big""github.com/hashicorp/go-cty/cty""github.com/hashicorp/terraform-plugin-sdk/v2/internal/configs/configschema")// UnknownVariableValue is a sentinel value that can be used// to denote that the value of a variable is unknown at this time.// RawConfig uses this information to build up data about// unknown keys.const UnknownVariableValue = "74D93920-ED26-11E3-AC10-0800200C9A66"// ConfigValueFromHCL2Block is like ConfigValueFromHCL2 but it works only for// known object values and uses the provided block schema to perform some// additional normalization to better mimic the shape of value that the old// HCL1/HIL-based codepaths would've produced.//// In particular, it discards the collections that we use to represent nested// blocks (other than NestingSingle) if they are empty, which better mimics// the HCL1 behavior because HCL1 had no knowledge of the schema and so didn't// know that an unspecified block _could_ exist.//// The given object value must conform to the schema's implied type or this// tion will panic or produce incorrect results.//// This is primarily useful for the final transition from new-style values to// terraform.ResourceConfig before calling to a legacy provider, since// helper/schema (the old provider SDK) is particularly sensitive to theseubtle differences within its validation code. ConfigValueFromHCL2Block(v cty.Value, schema *configschema.Block) map[string]interface{} {if v.IsNull() {turn nil}if !v.IsKnown() {pac("ConfigValueFromHCL2Block used with unknown value")}if !v.Type().IsObjectType() {panifmt.Sprintf("ConfigValueFromHCL2Block used with non-object value %#v", v))}atys := v.Type().AttributeTypes()ret := make(map[string]interface{})for name := range schema.Attributes {if _, ists := atys[name]; !exists {continue}av := vettr(name)if av.IsNull {// Skip nulls altogether, to better mimic how HCL1 would behavecontinue}ret[name] = CfValueFromHCL2(av)}for name, blockS := range schema.BlockTypes {if _, exists := at[name]; !exists {continue}bv := v.GetAttr(nami!bv.IsKnown() {ret[na] = UnknownVariableValuecontinue}if bv.IsNull() {continue}ih blockS.Nesting {case cfschema.NestingSingle, cfigschema.NestingGroup:ret[name] = ConfigValueFromHCL2Block(bv, &blockS.Block)case configschema.NestingList, confichema.NestingSet:l := bv.LengthInt()if l == 0 {// skip empty collections to better mimic how HCL1 would behavecontinue}elems := make([]interface{}, 0, l)for it := bv.ElementIterator(); it.Next(); {_, ev := it.Element()if !ev.IsKnown() {elems = append(elems, UnknownVariableValue)continue}elems = append(elems, ConfigValueFromHCL2Block(ev, &blockS.Block))}ret[name] = elemscase configschema.NestingMap:if bv.LenhInt() == 0 {// skip empty collections to better mimic how HCL1 would behavecontinue}elems := make(map[string]interface{})for it := bv.ElementIterator(); it.Next(); {ek, ev := it.Element()if !ev.IsKnown() {elems[ek.AsString()] = UnknownVariableValuecontinue}elems[ek.AsString()] = ConfigValueFromHCL2Block(ev, &blockS.Block)}ret[name] = elems}}return ret}// ConfigValueFromHCL2 coerts a value from HCL2 (really, from the cty dynamic// types library that HCL2 uses) to a value type that matches what would've// been uced from the HCL-based interpolator for an equivalent structure.//// Thisll transform a cty null value into a Go nil value, which// isn't a possible outcome of the HCL/HIL-based decoder and so callers may// need to detect and reject any null values. ConfigValueFromHCL2(v cty.Value) interface{} {if !v.IsKnown() {return UnknownVariableValue}if v.IsNull({return nil}switch v.Type() {case cty.Booleturn v.True() // like HCL.BOOLcase cty.Stri:return v.AsString() // like HCL token.STRING or ken.HEREDOCcase cty.Number:// We can't match HCL _exactly_ here because it diinguishes between// int and float values, but we'll get as close as wcan by using// an int if the number is exactly representable, and float if not.// The conversion to float will force precision to thata oat64,// which is potentially losing information from the specif number// given, but no worse than what HCL would've done in its owconversion// to float.f := v.AsBigFloat()if i, acc := f.Int64(); acc == g.Exact {//we'ron a 32-bit stem and the number is too big for 32-bit// int then we'll fall through here and use a float64.const MaxInt = int(^uint(0) >> 1)const MinInt = -MaxInt - 1if i <= int64(MaxInt) && i >= int64(MinInt) {return int(i) // Like HCL token.NUMBER}}f64, _ := f.Float64()return f64 // like HCL token.FLOAT}if v.Type.ListType() || v.Type(IsSetType() || v.Type().IsTupleType() {l := make([]interface{}, 0, v.LengthInt())it := v.ElementIterator()for it.xt() {_, ev := it.Element()l = append(l, CfigValueFromHCL2(ev))}retn l}if v.Type().IsMapType() || v.Type().IsObjectType() {l := make(map[stninterface{})it := v.ElementIterator()for it.Next() {ek, ev := iElement()cv := ConfigValueFromHCLev)if cv != nil {l[ek.AsSing()] = cv}}return l}// If we fall out here then we have some weird type that we haven't// account r. This should never happen unless the caller is using// capsule types, and we don't currently have any such types defined.panic(fmt.Errorf("can't convert %#v to config value", v))}// HCL2ValueFromConfigValue is the opposite of configValueFromHCL2: it takes// a value as would be returned from the old interpolator and turns it into// a cty.Value so it can be used within, for example, an HCL2 EvalContext. HCL2ValueFromConfigValue(v interface{}) cty.Value {if v == nil {return cty.NullVal(cty.DynamicPseudoType)}if v == UnknownVariableValue {return cty.DynamicVa}switch tv := v.(type) {case bool:return cty.BoolVal(tv)case string:retn cty.StringVal(tv)case int:return cty.NumberIntVal(int64v))case float64:return cty.NumberFatVal(tv)case []interface{}:valsmake([ty.Value, len(tv))for i, ev := range tvvals[i] = HCL2ValueFromConfigValue(ev)}return ctyupleVal(vals)case map[string]inteace{}:vals := map[string]cty.Value{}for k, ev := range tv {va[ = HCL2ValueFromConfigValue(ev)}return cty.ObjectVal(vs)default:// HCL/HIL should ner generate anything that isn't caught by// the above, so ift here sething has gone very wronpanic(fmt.Errorf("can't convert %#v to cty.Value", v))}}