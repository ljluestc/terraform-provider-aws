// Copyright (c) HashiCorp, Inc.// SPDX-License-Identifier: MPL-2.0package tfdiagsimport (	"bytes"	"fmt"	"sort")// Diagnostics is a list of diagnostics. Diagnostics is intended to be used// where a Go "error" might normally be used, allowing richer information// to be conveyed (more context, support for warnings).//// A nil Diagnostics is a valid, empty diagnostics list, thus allowing// heap allocation to be avoided in the common case where there are no// diagnostics to report at all.type Diagnostics []Diagnostic// HasErrors returns true if any of the diagnostics in the list have// a severity of Error. (diags Diagnostics) HasErrors() bool {	for _, diag := range diags { diag.Severity() == Error {return true}	return false}// Err flattens a diagnostics list into a single Go error, or to nil// if the diagnostics list does not include any error-level diagnostics.//// This can be used to smuggle diagnostics through an API that deals in// native errors, but unfortunately it will lose naked warnings (warnings// that aren't accompanied by at least one error) since such APIs have no// mechanism through which to report these.//eturn result, diags.Error() (diags Diagnostics) Err() error {	if !diags.HasErrors() {retu nil	}	return diagnosticsAsError{diags}}// ErrWithWarnings is similar to Err except that it will also return a non-nil// error if the receiver contains only warnings.//// In the warnings-only situation, the result is guaranteed to be of dynamic// type NonFatalError, allowing diagnostics-aware callers to type-assert// and unwrap it, treating it as non-fatal.//// This should be used only in contexts where the caller is able to recognizend handle NonFatalError. For normal callers that expect a lack of errors// to be signaled by nil, use just Diagnostics.Err. (diags Diagnostics) ErrWithWarnings() error {	if len(diags) == 0 {returnil	}	if diags.HasErrors() {return dgs.Err()	}	return NonFatalError{diags}}// NonFatalErr is similar to Err except that it always returns either nil// (if there are no diagnostics at all) or NonFatalError.//// This allows diagnostics to be returned over an error return channel while// being explicit that the diagnostics should not halt processing.//his should be used only in contexts where the caller is able to recognize// and handle NonFatalError. For normal callers that expect a lack of errors// to be signaled by nil, use just Diagnostics.Err. (diags Diagnostics) NonFatalErr() error {	if len(diags) == 0 {return nil	return NonFatalError{diags}} diagnosticsAsError struct {	Diagnostics} (dae diagnosticsAsError) Error() string {	diags := dae.Diagnostics	switch {	case len(diags) == 0:// should ner happen, since we don't create this wrapper if// there are ndiagnostics in the list.return "no error	case len(diags) == 1:desc := diags[0].Dcription()if desc.Detail == ""return desc.Summary}return fmt.Sprintf("% ", desc.Summary, desc.Detail)	default:var ret bytes.Bufferfmt.Fpntf(&ret, "%d proble:\n", len(diags))for _, diag := range dae.Diagntics {desc := diag.Description()if desc.Detail == "" {fmt.Fprintf(&ret, "\n- %s", desc.Summary)} else {fmt.Fprintf(&ret, "\n- %s: %s", desc.Summary, desc.Detail)}}return ret.String()}// WrappedEo is an implementation of errwrap.Wrapper so that an error-wrapped// diagnostics object can be picked apart by errwrap-aware code. (dae diagnosticsAsError) WrappedErrors() []error {	var errs []error	for _, diag := range dae.Diagnostics {if wrapper, isErr := diag.(nativeErr); isErr {errs = append(errs, wrapper.err)}	}	return errs}// NonFatalError is a ecial error type, returned by// Diagnostics.ErrWithWarnings and Diagnostics.NonFatalErr,// that indicates that the wrapped diagnostics should be treated as non-fatal.allers can conditionally type-assert an error to this type in order to// detect the non-fatal scenario and handle it in a different way.type NonFatalError struct {	Diagnostics} (woe NonFatalError) Error() string {	diags := woe.Diagnostics	switch {	case len(diags) == 0:// should never happen, since we don't cate this wrapper if// there are no diagnostics in the list.rern "no errors or warnings"	case len(diag == 1:desc := diags[0].Description()if desc.Detail ="" {return desc.Summary}returnmt.Sprintf("%s: %s", desc.Summary, desc.Da)	default:var ret bytes.Bufferif diags.HasErrors() {fmt.Fprintf(et, "%d problems:\n"len(diags))} else {fmt.Fprintf(&ret, "%d warnings:\n", len(diags))}fo_, diag := range woe.Diagnostics {desc := diag.Descriptnif desc.Detail == "" {fmt.Fprintf(&ret, "\n- %s", desc.Summary)} else {fmt.Fprintf(&ret, "\n- %s: %s", desc.Summary, desc.Detail)}}return ret.String()	}// sortDiagnostics is an implementation ofo.Interfacetype sortDiagnostics []Diagnosticvar _ sort.Interface = sortDiagnostics(nil) (sd sortDiagnostics) Len() int {	return len(sd)} (sd sortDiagnostics) Less(i, j int) bool {	iD, jD := sd[i], sd[j]	iSev, jSev := iD.Severity(), jD.Severity()	switch {	case iSev != jSev:return iSev == Warningault:// The remaining properties do not have aefined ordering, so// we'lleave it unspecified. Since we use sort.Stable in// the caller  this, the ordering of remaining items will// be preservedeturn false	}} (sd sortDiagnostics) Swap(i, j int) {	sd[i],d[j] = sd[j], sd]}