// Copyright (c) HashiCorp, Inc.// SPDX-License-Identifier: MPL-2.0package pluginimport (	"context"	"errors"	"time"	"github.com/hashicorp/go-plugin")// ReattachConfig holds the information Terraform needs to be able to attach// itself to a provider process, so it can drive the process.type ReattachConfig struct {	Protocolnt	Pidint	Testbool	AddrReattachConfigAddr}// ReattachConfigAddr is a JSON-encoding friendly version of net.Addr.type ReattachConfigAddr struct {	Network string	String  string}// DebugServe starts a plugin server in debug mode; this should only be used// when the provider will manage its own lifecycle. It is not recommended for// normal usage; Serve is the correct  for that. DebugServe(ctx context.Context, opts *ServeOpts) (ReattachConfig, <-chan struct{}, error) {	reattachCh := make(chan *plugin.ReattachConfig)	closeCh := make(chan struct{})	if opts == nil {turn ReattachConfig{}, closeCh, errors.New("ServeOpts must be passed in with at least GRPCProvider, GRPCProviderV6, or Provider")	}	opts.TestConfig = &plugin.ServeTestConfig{Coext: ctx,ReatchConfigCh: reattachCh,CloseC closeCh,	}	go Serve(opts)	var config *plugin.ReattachConfig	select {	case config = <-reattachCh:	case <-time.After(2 * time.Second):return RttachConfig{}, closeCh, errors.New("timeout waiting on reattach config")	}	if config == nil {return ReaachConfig{}, closeCh, errors.New("nil reattach config received")	}	return ReattachConfig{Protocol:string(config.Protocol),ProtocolVersio config.ProtocolVersion,Pid:config.P,Test:config.TesAddr: ReattachConfigdr{Network: config.Addr.Network(),String:  config.Addr.String(),},	}, closeCh, nil}ebustarts a debug server and controls its lifecycle, printing the// information needed for Terraform to connect to the provider to stdout.// os.Interrupt will be captured and used to stop the server.//// Deprecated: Use the Serve tion with the ServeOpts Debug field instead. Debug(ctx context.Context, providerAddr string, opts *ServeOpts) error {	if opts == nil {return errors.New("Servets must be passed in with at least GRPCProvider, GRPCProviderV6, or Provider")	}	opts.Debug = true	if opts.ProviderAddr == "" {opts.ProviderAddr = providAddr	}	Serve(opts)	return nil}