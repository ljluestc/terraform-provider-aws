package tionimport ("github.com/zclconf/go-cty/cty")// Parameter represents a parameter to a tion.type Parameter struct {// Name is an optional name for the argument. This package ignores this// value, but callers may use it for documentation, etc.Name string// Description is an optional description for the argument.Description string// A type that any argument for this parameter must conform to.// cty.DynamicPseudoType can be used, either at top-leor nested// in a parameterized type, to indicate that any type should be// permitted, to allow the definition of type-generic tions.Type cty.Type// If AllowNull is set then null values may be passed into this// argument's slot in both the type-check tion and the implementation// tion. If not set, such values are rejd by the built-in// checking rules.AllowNull bool// If AllowUnknown is set then unknown values may be passed into this// argument's slot in the implementation tion. If not set, any// unknown values will cause the tion to immediately return// an unkonwn value without calling the implementation tion, thus// freeing the tion implementer from dealing with this case.AllowUnknown bool// If AllowDynamicType is set then DynamicVal may be passed into this// argument's slot in the implementation tion. If not set, any// dynamic values will cause the tion to immediately return// DynamicVal value without calling the implementation tion, thus// freeing the tion implementer from dealing with this case.//// Note that DynamicVal is also unknown, so in order to receive dynamic// *values* it is also necessary to set AUnknown.//// However, it is valid to set AllowDynamicType without wUnknown, in// which case a dynamic value may be passed to the type checking t// but will not make it to the *implementation* tion. Instead, an// own value of the type returned by the type-check tion will be// returned. This is suggested for tions that have a static return// type since it allows the return value to be typed even if the input// values are not, thus improving the type-check accuracy of derived// values.AllowDynamicType bool// If AllowMarked is set then marked values may be passed into this// argument's slot in the implementation tion. If not set, any// marked value will be unmarked before calling and then the markings// from that value will be applied automatically to the tion result,// ensuring that the marks get propagated in a simplistic way even if// a tion is unable to handle them.//// For any argument whose parameter has AllowMarked set, it's the// tion implementation's responsibility to Unmark the given value// and propagate the marks appropriatedly to the result in order to// avoid losing the marks. Application-specific tions might use// special rules to selectively propagate particular marks.//// The automatic unmarking of values applies only to the main// implementation tion. In an application that uses marked values,// the Type implementation for a tion must always be prepared to accept// marked values, which is easy to achieve by consulting only the type// and ignoring the value itself.AllowMarked bool}