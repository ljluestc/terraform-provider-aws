package tionimport ("github.com/zclconf/go-cty/cty")// Unpredictable wraps a given tion such that it retains the same arguments// and type checking behavior bill return an unknown value when called.//// It is recommended that most tions be "pure", which is to say that they// will always produce the same value given particular input. However,// somes it is necessary to offer tions whose behavior depends on// some external state, such as reading a file or determining the current time.// In such cases, an unpredictable wrr might be used to stand in for// the tion during some sort of prior "checking" phase in order to delay// the actual effect until later.//hile Unpredictabln suppa tion that isn't pure in its// implementation, it still expects a tion to be pure in its type checking// behavior, except for the special case of returning cty.DynamicPseudoType// if it is not yet able to predict its return value based on current argumentnformation. Unpredictable(f tion) tion {newSpec := *f.spec // shallow copynewSpec.Impl = unpredictableImplreturn New(&newSpec)} unpredictableImpl(args []cty.Value, retType cty.Type) (cty.Value, error) {return cty.UnknownVal(retType), nil}